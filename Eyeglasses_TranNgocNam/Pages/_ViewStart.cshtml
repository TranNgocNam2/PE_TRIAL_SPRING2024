@{
    Layout = "_Layout";
}
@{
    // var isAuthenticated = !string.IsNullOrEmpty(Context.Session.GetString("Email"));
    // if (!isAuthenticated && !Context.Request.Path.StartsWithSegments("/"))
    // {
    //     Context.Response.Redirect("/");
    // }
}
